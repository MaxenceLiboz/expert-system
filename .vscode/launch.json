{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "preLaunchTask": "build",
            "program": "${workspaceFolder}/expert-system",
            "args": [
            ],
            "cwd": "${workspaceFolder}/srcs"
        },
    ]
}